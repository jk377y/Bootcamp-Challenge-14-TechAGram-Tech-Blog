<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Document</title>
    <link rel="stylesheet" href="./css/style.css"><link rel="preconnect" href="https://fonts.googleapis.com">
</head>
<body class="body">


    <header class="header">
        <h1 class="title"><span class="span">tech<span class="spanA">A</span>gram</span></h1>
        <nav class="nav">
            <H2 id="home" class="home"><a href="/">Home</a></H2>
            <h2 id="dashboard" class="dashboard"><a href="/dashboard">Dashboard</a></h2>
            {{#if loggedIn}}
            <h2 id="logout" class="logout" ><a href="/logout">Logout</a></h2>
            {{else}}
            <h2 id="login" class="login"><a href="/login">Login</a></h2>
            {{/if}}
        </nav>
        
    </header>
    
    <main>
        {{#if loggedIn}}
        <h3 id="greet" class="greet">Hello, James</h3>
        {{/if}}
        
        {{{body}}}
    </main>
    {{#if loggedIn}}
    <script src="./js/logout.js"></script>
    {{/if}}
</body>
</html>